import { IFormData } from "./Form"

interface IProps {
    item: Array<IFormData>
}

export function Board (props:IProps) {
    
   console.log(props)
    return (
        <div>

        </div>
    )
}